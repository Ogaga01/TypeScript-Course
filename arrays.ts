const myArray: string[] = [] // Declaring an array of srtings
const myNumberArray: number[] = [] // Array of numbers
type User = {
    name: string
    isActive: boolean
}
const objectArray: User[] = [] //Array of objects

myArray.push('1')
myNumberArray.push(45)


export {}